# 🏙️ Wplace Timelapse - Томск

Автоматическая система для создания таймлапса центра Томска на основе данных с wplace.live с использованием GitHub Actions и GitHub Pages.

## 🎯 Возможности

- **Автоматический захват**: Каждые 5 минут загружает 9 тайлов (3×3 сетка) с wplace.live
- **Объединение изображений**: Автоматически склеивает тайлы в одно большое изображение
- **Таймлапс видео**: Создает MP4 видео и GIF анимацию из собранных кадров
- **Веб-интерфейс**: Красивая страница для просмотра последних изображений и таймлапса
- **GitHub Pages**: Автоматическая публикация на GitHub Pages

## 🚀 Быстрый старт

### 1. Настройка репозитория

1. **Fork этого репозитория** или создайте новый репозиторий
2. **Клонируйте репозиторий**:
   ```bash
   git clone https://github.com/YOUR_USERNAME/wplace-tomsk-timelapse.git
   cd wplace-tomsk-timelapse
   ```

### 2. Настройка GitHub Actions

1. Перейдите в настройки репозитория: **Settings** → **Actions** → **General**
2. Убедитесь, что включены:
   - ✅ "Allow all actions and reusable workflows"
   - ✅ "Read and write permissions" для GITHUB_TOKEN

### 3. Настройка GitHub Pages

1. Перейдите в **Settings** → **Pages**
2. В разделе **Source** выберите **GitHub Actions**
3. Сохраните настройки

### 4. Запуск системы

1. **Первый запуск** (вручную):
   - Перейдите во вкладку **Actions**
   - Выберите workflow **"Wplace Timelapse"**
   - Нажмите **"Run workflow"** → **"Run workflow"**

2. **Автоматический режим**:
   - После первого запуска система будет работать автоматически
   - Новые изображения будут захватываться каждые 5 минут
   - Сайт будет обновляться автоматически

### 5. Просмотр результатов

Ваш сайт будет доступен по адресу:
```
https://YOUR_USERNAME.github.io/wplace-tomsk-timelapse/
```

## 📁 Структура проекта

```
├── .github/workflows/
│   └── timelapse.yml          # GitHub Actions workflow
├── docs/                      # GitHub Pages сайт
│   ├── index.html            # Главная страница
│   ├── images/               # Папка с изображениями
│   ├── latest.png            # Последнее изображение
│   ├── latest.json           # Метаданные последнего изображения
│   ├── timelapse.mp4         # Видео таймлапс
│   ├── timelapse.gif         # GIF таймлапс
│   └── timelapse_info.json   # Информация о таймлапсе
├── images/                   # Архив всех изображений
├── tiles/                    # Временные тайлы
├── capture_tiles.py          # Скрипт захвата тайлов
├── generate_timelapse.py     # Скрипт создания таймлапса
├── requirements.txt          # Python зависимости
└── README.md                # Этот файл
```

## ⚙️ Настройка

### Изменение интервала захвата

Отредактируйте файл `.github/workflows/timelapse.yml`:

```yaml
on:
  schedule:
    # Для изменения интервала измените cron выражение
    - cron: '*/5 * * * *'  # Каждые 5 минут
    # - cron: '*/10 * * * *'  # Каждые 10 минут
    # - cron: '0 * * * *'     # Каждый час
```

### Изменение координат тайлов

Отредактируйте файл `capture_tiles.py`, изменив переменную `TILE_COORDS`:

```python
# Текущие координаты (3x3 сетка центра Томска)
TILE_COORDS = [
    (1506, 631), (1507, 631), (1508, 631),
    (1506, 632), (1507, 632), (1508, 632),
    (1506, 633), (1507, 633), (1508, 633)
]
```

### Настройка качества видео

В файле `generate_timelapse.py` можно изменить параметры ffmpeg:

```python
# Для более высокого качества
'-crf', '18',  # Вместо '23'

# Для другой частоты кадров
'-vf', 'fps=10,scale=768:768:flags=lanczos',  # 10 FPS вместо 5
```

## 🛠️ Локальная разработка

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Тестирование скриптов

```bash
# Захват одного изображения
python capture_tiles.py

# Создание таймлапса (если есть несколько изображений)
python generate_timelapse.py
```

### Локальный веб-сервер

```bash
# Запуск простого HTTP сервера для тестирования сайта
cd docs
python -m http.server 8000

# Откройте http://localhost:8000
```

## 📊 Мониторинг

### Проверка статуса workflow

1. Перейдите во вкладку **Actions** вашего репозитория
2. Проверьте последние запуски workflow **"Wplace Timelapse"**
3. Зеленая галочка ✅ означает успешное выполнение
4. Красный крестик ❌ означает ошибку (кликните для деталей)

### Анализ ошибок

Типичные проблемы и решения:

1. **403 Forbidden при деплое**:
   - Проверьте права GITHUB_TOKEN в настройках Actions
   - Убедитесь, что GitHub Pages настроен правильно

2. **Ошибка загрузки тайлов**:
   - Проверьте доступность wplace.live
   - Убедитесь в правильности координат тайлов

3. **Ошибка создания видео**:
   - В GitHub Actions ffmpeg установлен по умолчанию
   - При ошибке создается fallback GIF

## 🎨 Кастомизация веб-интерфейса

### Изменение внешнего вида

Отредактируйте CSS в файле `docs/index.html`:

```css
/* Изменение цветовой схемы */
body {
    background: linear-gradient(135deg, #your-color1 0%, #your-color2 100%);
}

/* Изменение заголовка */
h1 {
    font-size: 3em;
    color: #your-color;
}
```

### Добавление функций

JavaScript код находится в том же файле `docs/index.html`. Вы можете:

- Добавить новые кнопки управления
- Изменить логику автообновления
- Добавить дополнительную аналитику

## 📈 Производительность

### Оптимизация размера репозитория

GitHub имеет ограничения на размер репозитория. Для больших таймлапсов:

1. **Используйте Git LFS** для больших файлов:
   ```bash
   git lfs track "*.mp4"
   git lfs track "docs/images/*.png"
   ```

2. **Периодически очищайте старые изображения**:
   ```python
   # Добавьте в capture_tiles.py логику для удаления старых файлов
   if len(images) > 1000:  # Храним только последние 1000 изображений
       # Удаляем старые файлы
   ```

### Мониторинг использования Actions

- GitHub предоставляет 2000 минут Actions в месяц для бесплатных аккаунтов
- При захвате каждые 5 минут: ~8640 запусков в месяц
- Каждый запуск ~2-3 минуты = ~20000 минут в месяц
- **Рекомендация**: Используйте интервал 10-15 минут для экономии

## 🤝 Вклад в проект

1. Fork репозитория
2. Создайте feature branch: `git checkout -b feature/amazing-feature`
3. Commit изменения: `git commit -m 'Add amazing feature'`
4. Push в branch: `git push origin feature/amazing-feature`
5. Создайте Pull Request

## 📝 Лицензия

Этот проект распространяется под лицензией MIT. См. файл `LICENSE` для деталей.

## 🙏 Благодарности

- [wplace.live](https://wplace.live) за предоставление данных
- GitHub за Actions и Pages
- Сообщество разработчиков Томска

---

**Автор**: [Ваше имя]  
**Контакт**: [ваш email]  
**Сайт**: https://YOUR_USERNAME.github.io/wplace-tomsk-timelapse/
